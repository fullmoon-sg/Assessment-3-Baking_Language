@layout('base')
@section('content')
<form method="POST">
  {{csrfField()}}
  <h1>New Customer Details</h1>
  <div>
  <label class="form-label">First Name </label>
  <input type="text" name="firstname" value="{{old('firstname','')}}" class="form-control mb-3"/>
    @if(hasErrorFor('firstname'))
    <span> {{ getErrorFor('firstname') }} </span>
    @endif
  </div>
   <div>
  <label class="form-label">Last Name </label>
  <input type="text" name="lastname" value="{{old('lastname','')}}" class="form-control mb-3"/>
   @if(hasErrorFor('lastname'))
    <span> {{ getErrorFor('lastname') }} </span>
    @endif
  </div>
   <div>
  <label class="form-label">Gender </label>
  <select class="form-control" name="gender">
    <option value="Male">Male</option>
    <option value="Female">Female</option>
  </select>
  </div>
   <div>
  <label class="form-label">Email </label>
  <input type="email" name="email" value="{{old('email','')}}" class="form-control mb-3"/>
  @if(hasErrorFor('email'))
    <span> {{ getErrorFor('email') }} </span>
    @endif
  </div>
   <div>
  <label class="form-label">Mobile Number</label>
  <input type="number" name="mobile_number" value="{{old('mobile_number','')}}" class="form-control mb-3"/>
   @if(hasErrorFor('mobile_number'))
    <span> {{ getErrorFor('mobile_number') }} </span>
    @endif
</div>
   <div>
  <label class="form-label">Address </label>
  <input type="text" name="address" value="{{old('address','')}}" class="form-control mb-3"/>
   @if(hasErrorFor('address'))
    <span> {{ getErrorFor('address') }} </span>
    @endif
  </div>
   <div>
  <label class="form-label">Select a username </label>
  <input type="text" name="username" class="form-control mb-3"/>
   @if(hasErrorFor('username'))
  <span> {{ getErrorFor('username') }} </span>
    @endif
  </div>
   <div>
  <label class="form-label">Select a password </label>
  <input type="password" name="password" class="form-control mb-3"/>
  @if(hasErrorFor('password'))
  <span> {{ getErrorFor('password') }} </span>
    @endif
  </div>
  <input type="submit" value="Create" class="btn btn-primary mt-3"/>
</form>
@endsection
