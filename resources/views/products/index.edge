@layout('base')
@section('content')

<h1>Display all Products</h1>
 <a href="{{route('create_product')}}" class="btn btn-success mb-3">Create New Product</a>
<div class="row">
@each(p in products)
<div class="card" style="width: 15rem;">
  <img class="card-img-top" src="{{p.image_url}}" alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title">{{p.category}}</h5>
    <p class="card-text">{{p.description}}</p>
     <p class="card-text">${{p.price/100}}</p>
   <a href="{{route('add_to_order',{product_id:p.id})}}" class="btn btn-success">Add To Order</a>
    <a href="{{route('add_feedback', {product_id:p.id})}}" class="btn btn-success mt-2 mb-2">Leave Feedback</a>
    <a href="{{route('update_product', {product_id:p.id})}}" class="btn btn-success mt-2 mb-2">Edit Product</a>
    <a href="{{route('delete_product',{product_id:p.id})}}" class="btn btn-danger mt-2 mb-2">Remove Product</a>
   <div class="card-footer">
      <small class="text-muted ">Updated : {{p.updated_at}} </small>
    </div>
  </div>
</div>
 @endeach
</div>
@endsection
